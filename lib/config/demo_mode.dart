/// DEMO MODE — flip to `false` after the squad meeting to restore the full UI.
///
/// When true:
///   - Hides crypto/giftcard quick actions on the dashboard and wallet screens.
///   - Hides the crypto portfolio section on the dashboard.
///
/// This is a temporary flag for the squad meeting demo. Set to `false` once
/// the meeting is over and live API access is granted.
const bool kDemoMode = false;
