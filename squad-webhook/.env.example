# Squad Webhook Environment Variables

# Firebase service account key (JSON string)
# Generate from: Firebase Console → Project Settings → Service Accounts → Generate new private key
# Then paste the entire JSON content as a single line (minified)
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"katrexapp-83cde",...}
